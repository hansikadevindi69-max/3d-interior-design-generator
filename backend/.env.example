# Server
PORT=4000
NODE_ENV=development

# Database
DATABASE_PATH=./data/app.db

# File uploads
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE_MB=25

# AI Providers (optional - falls back to built-in mock generator when unset)
OPENAI_API_KEY=
STABILITY_API_KEY=
AI_PROVIDER=mock
